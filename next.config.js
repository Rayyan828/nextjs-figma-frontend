/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: [], // Add Figma asset domains here if used
    },
  };
  
  module.exports = nextConfig;